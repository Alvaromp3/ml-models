# Graceland Soccer (moved)

The **Graceland Soccer web application** now lives in its own repository:

**https://github.com/Alvaromp3/graceland_soccer**

Use that repo for issues, pull requests, Render deploys (`render.yaml` at the root), and day-to-day development.

This folder under `ml-models` may lag behind or be removed later; treat **`graceland_soccer`** as the source of truth.
